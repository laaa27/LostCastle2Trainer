# 更新日志 / Changelog

## v1.1.0 — 原生窗口面板 (2026-08-17)

自 v1.0.0（浏览器面板）以来，修改器 UI 从本地网页迁移为 **Windows 原生窗口面板**，并围绕可用性做了一系列修复与优化。

### 新增

- **原生 WinForms 窗口面板** (`winui/WinPanel.exe`) 取代浏览器面板：直接双击 exe 启动，无需打开网页。
- **面板自举**：启动时自动检查 Node / `node_modules` / 编译产物，缺失时自动执行 `npm install` 与 `npm run build`，无需任何命令行。
- **全局热键呼出/隐藏**：游戏中按 **反引号键 `` ` ``** 随时呼出 / 收起面板（置顶显示）。
- **host 自动托管**：面板自动拉起 host.js 连接游戏进程并维护其生命周期。
- **完整三大功能区**：资源修改、物品搜索与生成掉落、属性读取/修改/重置。
- **游戏关闭自动退出**：检测到 `LostCastle2` 进程结束后面板自动停止 host 并退出。

### 修复

- **热键间歇失效**：添加单实例（Mutex）保护，重复双击只激活已有窗口；`RegisterHotKey` 失败会告警并写日志，不再静默失效。
- **端口占用自动回退**：默认 8899 被占时依次尝试 9599 → 9800 → 随机端口，面板从 host 输出解析实际监听端口。
- **启动器编码问题**：`start-trainer.bat` 改英文文件名，规避 cmd 中文文件名 + GBK 编码执行失败。
- **面板响应解析**：支持 JSON 布尔值 / `null`，修复属性等字段解析异常。
- **host.js 路径修正**：原生面板正确解析 `winui\` 上级目录定位仓库根的 `host.js`。

### 优化

- **启动提速**：面板先行显示（主窗口 ~0.7s 出现，原先同步等待 host 约 7s），host 在后台线程启动，就绪后自动刷新数据。
- **host 探测提速**：快速 TCP 连接预检跳过不可用端口，避免每次探测等待系统超时。
- **免 node 环境打包**：新增 `winui/package-release.ps1` 一键打包 Release zip（内置便携 Node.js + node_modules），用户解压后双击即用、零安装零配置；面板优先使用包内 node。
- **提示条移除**：顶部黄色横幅移除，提示移入标题栏，不再遮挡功能区。
- **窗口交互简化**：移除标题栏最小化 / 最大化 / 关闭按钮，底部提供「关闭修改器」按钮（一并停止 host）。

### 技术

- 面板构建使用系统自带 `csc.exe`（`winui/build-ui.ps1`），零额外运行时依赖。
- 诊断脚本 `diag.js` 保留：attach → 各 RPC → 干净 detach，用于手动回归验证。

---

## v1.0.0 — 浏览器面板

- Frida + frida-il2cpp-bridge 核心注入层，新旧版本游戏自动兼容（逐符号探测 + 语义校验 + 结果缓存）。
- 本地网页面板：资源修改 / 物品搜索生成 / 属性修改重置。